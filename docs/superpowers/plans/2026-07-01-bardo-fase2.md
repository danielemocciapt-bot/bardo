# Bardo — Fase 2 (Home, libreria scene, audio background) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Home con 4 scene navigabili (tasto Indietro Android torna alla Home), audio CC0 reale per le 4 scene con offline via precache, pausa vera, e riproduzione controllabile da lock screen (MediaSession) + Wake Lock.

**Architecture:** Uno store `route` (History API) sceglie tra `Home` e `GameScreen`. L'`AudioEngine` resta il wrapper Howler (musica in modalità HTML5 per il background; ambient/SFX in Web Audio); lo store `mixer` orchestra engine + due wrapper isolati e feature-detected: `media-session` (metadata + handler lock screen) e `wake-lock` (tieni schermo acceso). I dati diventano una libreria di 4 scene.

**Tech Stack:** Svelte 4, Vite 5, Howler.js, Vitest 1.6 (Howl e API browser mockati), vite-plugin-pwa (Workbox precache esteso agli audio).

**Spec:** `docs/superpowers/specs/2026-07-01-bardo-fase2-design.md`
**Base:** Fase 1 su `master`. Codice esistente: `AudioEngine.js`, `mixer.js`, `scenes.js` (singola `demoScene`), `GameScreen.svelte` + componenti, `vite.config.js`.

---

## File Structure (Fase 2)

```
Nuovi:
  src/lib/stores/route.js            # routing Home/Game su History API
  src/lib/stores/route.test.js
  src/lib/audio/media-session.js     # wrapper MediaSession (feature-detected)
  src/lib/audio/media-session.test.js
  src/lib/audio/wake-lock.js         # wrapper Wake Lock (feature-detected)
  src/lib/audio/wake-lock.test.js
  src/components/Home.svelte          # griglia scene
  src/components/SceneCard.svelte     # tessera scena
  src/lib/data/licenses.json          # registro licenze audio
  public/audio/<sceneId>/*            # audio CC0 reale

Modificati:
  src/lib/data/scenes.js              # -> `scenes` (4) + getScene + campo cover
  src/lib/types.js                    # Scene.cover
  src/lib/audio/AudioEngine.js        # pause(), html5 per-layer, oneshot unload
  src/lib/stores/mixer.js             # togglePlay=pause/resume; deps media/wake; keepAwake
  src/components/GameScreen.svelte     # scena via sceneId; costruisce media/wake; back a Home; toggle Wake Lock
  src/App.svelte                       # Home o GameScreen in base a $route
  vite.config.js                       # precache audio (globPatterns)
```

**Convenzioni invariate:** JS + JSDoc, Svelte 4, volume effettivo = master*layer. Test con Vitest globals + jsdom.

---

## Task 1: Libreria scene (4 scene) + getScene + cover

**Files:**
- Modify: `src/lib/types.js`, `src/lib/data/scenes.js`
- Modify test: `src/lib/data/scenes.test.js`

Nota: manteniamo `export const demoScene = scenes[0]` per non rompere i test/import esistenti di AudioEngine/mixer (che importano `demoScene`).

- [ ] **Step 1: Aggiorna il test** `src/lib/data/scenes.test.js` (sostituisci l'intero file):

```js
import { describe, it, expect } from 'vitest';
import { scenes, getScene, demoScene } from './scenes.js';

describe('scenes library', () => {
  it('contiene 4 scene con id/nome/cover e tre intensità', () => {
    expect(scenes.length).toBe(4);
    for (const s of scenes) {
      expect(s.id).toBeTypeOf('string');
      expect(s.name).toBeTypeOf('string');
      expect(s.cover).toBeTypeOf('string');
      expect(s.music.explore.length).toBeGreaterThan(0);
      expect(s.music.combat.length).toBeGreaterThan(0);
      expect(s.music.victory.length).toBeGreaterThan(0);
      expect(s.ambient.length).toBeGreaterThan(0);
      expect(s.oneshots.length).toBeGreaterThan(0);
    }
  });

  it('ha id scena unici', () => {
    const ids = scenes.map((s) => s.id);
    expect(new Set(ids).size).toBe(ids.length);
  });

  it('getScene ritorna la scena per id, undefined se assente', () => {
    expect(getScene('tavern')).toBe(scenes.find((s) => s.id === 'tavern'));
    expect(getScene('nope')).toBeUndefined();
  });

  it('demoScene è la prima scena', () => {
    expect(demoScene).toBe(scenes[0]);
  });
});
```

- [ ] **Step 2: Esegui e verifica fallimento**

Run: `npm test`
Expected: FAIL — `scenes`/`getScene` non esistono.

- [ ] **Step 3: Aggiorna `src/lib/types.js`** — aggiungi `cover` a `Scene` (sostituisci il typedef Scene):

```js
/**
 * @typedef {Object} Scene
 * @property {string} id
 * @property {string} name
 * @property {string} cover        // gradiente CSS o percorso illustrazione, usato da tessere/NowPlaying
 * @property {{explore: AudioRef[], combat: AudioRef[], victory: AudioRef[]}} music
 * @property {AudioRef[]} ambient
 * @property {AudioRef[]} oneshots
 */
```

- [ ] **Step 4: Riscrivi `src/lib/data/scenes.js`**

```js
/** @typedef {import('../types.js').Scene} Scene */

/** @param {string} id */
const two = (id, base) => [`/audio/${base}/${id}.webm`, `/audio/${base}/${id}.m4a`];

/** @type {Scene[]} */
export const scenes = [
  {
    id: 'tavern',
    name: 'Taverna del Drago',
    cover: 'linear-gradient(160deg,#f3c98a,#b98a4a)',
    music: {
      explore: [{ id: 'tavern-explore', name: 'Calma', src: two('explore', 'tavern'), loop: true }],
      combat:  [{ id: 'tavern-combat',  name: 'Rissa', src: two('combat', 'tavern'),  loop: true }],
      victory: [{ id: 'tavern-victory', name: 'Brindisi', src: two('victory', 'tavern'), loop: true }]
    },
    ambient: [
      { id: 'crowd', name: 'Brusio', src: two('crowd', 'tavern'), loop: true },
      { id: 'fire',  name: 'Fuoco',  src: two('fire', 'tavern'),  loop: true }
    ],
    oneshots: [
      { id: 'door',   name: 'Porta',    src: two('door', 'tavern') },
      { id: 'cheers', name: 'Applausi', src: two('cheers', 'tavern') }
    ]
  },
  {
    id: 'forest',
    name: 'Foresta Antica',
    cover: 'linear-gradient(160deg,#a9d18a,#6fa15a)',
    music: {
      explore: [{ id: 'forest-explore', name: 'Sentiero', src: two('explore', 'forest'), loop: true }],
      combat:  [{ id: 'forest-combat',  name: 'Agguato',  src: two('combat', 'forest'),  loop: true }],
      victory: [{ id: 'forest-victory', name: 'Radura',   src: two('victory', 'forest'), loop: true }]
    },
    ambient: [
      { id: 'wind',  name: 'Vento',   src: two('wind', 'forest'),  loop: true },
      { id: 'birds', name: 'Uccelli', src: two('birds', 'forest'), loop: true }
    ],
    oneshots: [
      { id: 'twig',    name: 'Ramo',  src: two('twig', 'forest') },
      { id: 'owl',     name: 'Gufo',  src: two('owl', 'forest') }
    ]
  },
  {
    id: 'city',
    name: 'Città Reale',
    cover: 'linear-gradient(160deg,#8fa6d1,#5d6fa1)',
    music: {
      explore: [{ id: 'city-explore', name: 'Vie', src: two('explore', 'city'), loop: true }],
      combat:  [{ id: 'city-combat',  name: 'Guardie', src: two('combat', 'city'), loop: true }],
      victory: [{ id: 'city-victory', name: 'Corte', src: two('victory', 'city'), loop: true }]
    },
    ambient: [
      { id: 'market', name: 'Mercato', src: two('market', 'city'), loop: true },
      { id: 'people', name: 'Folla',   src: two('people', 'city'), loop: true }
    ],
    oneshots: [
      { id: 'bell', name: 'Campana', src: two('bell', 'city') },
      { id: 'cart', name: 'Carro',   src: two('cart', 'city') }
    ]
  },
  {
    id: 'dungeon',
    name: 'Sotterranei',
    cover: 'linear-gradient(160deg,#6b5563,#3a2c38)',
    music: {
      explore: [{ id: 'dungeon-explore', name: 'Cripta', src: two('explore', 'dungeon'), loop: true }],
      combat:  [{ id: 'dungeon-combat',  name: 'Orrore', src: two('combat', 'dungeon'), loop: true }],
      victory: [{ id: 'dungeon-victory', name: 'Tesoro', src: two('victory', 'dungeon'), loop: true }]
    },
    ambient: [
      { id: 'drip', name: 'Gocce', src: two('drip', 'dungeon'), loop: true },
      { id: 'wind', name: 'Vento', src: two('wind', 'dungeon'), loop: true }
    ],
    oneshots: [
      { id: 'chain', name: 'Catene', src: two('chain', 'dungeon') },
      { id: 'growl', name: 'Ringhio', src: two('growl', 'dungeon') }
    ]
  }
];

/** @param {string} id @returns {Scene|undefined} */
export const getScene = (id) => scenes.find((s) => s.id === id);

/** Prima scena, usata come default in test e avvio. */
export const demoScene = scenes[0];
```

- [ ] **Step 5: Esegui i test**

Run: `npm test`
Expected: PASS. I test esistenti di AudioEngine/mixer usano `demoScene` (ora = tavern) e restano verdi. (Nota: `demoScene.ambient` ha ancora `crowd` e `fire`, quindi i test AudioEngine/mixer che citano quegli id continuano a valere.)

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: libreria di 4 scene con getScene e campo cover"
```

---

## Task 2: Store route (History API)

**Files:**
- Create: `src/lib/stores/route.js`
- Test: `src/lib/stores/route.test.js`

Store che sceglie Home/Game e integra il tasto Indietro. Riceve `history` e un target per `popstate` iniettabili per il test.

- [ ] **Step 1: Scrivi il test** `src/lib/stores/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { get } from 'svelte/store';
import { createRoute } from './route.js';

function fakeHistory() {
  return { pushState: vi.fn(), back: vi.fn() };
}
function fakeWindow() {
  const handlers = {};
  return {
    addEventListener: (type, cb) => { handlers[type] = cb; },
    _emit: (type, ev) => handlers[type] && handlers[type](ev),
  };
}

describe('route store', () => {
  let history, win, route;
  beforeEach(() => {
    history = fakeHistory();
    win = fakeWindow();
    route = createRoute({ history, target: win });
  });

  it('parte dalla Home', () => {
    expect(get(route).view).toBe('home');
  });

  it('open passa a game + pushState', () => {
    route.open('forest');
    expect(get(route)).toEqual({ view: 'game', sceneId: 'forest' });
    expect(history.pushState).toHaveBeenCalledWith({ sceneId: 'forest' }, '', expect.anything());
  });

  it('home chiama history.back', () => {
    route.open('forest');
    route.home();
    expect(history.back).toHaveBeenCalled();
  });

  it('popstate riporta lo store alla Home', () => {
    route.open('forest');
    win._emit('popstate', { state: null });
    expect(get(route).view).toBe('home');
  });
});
```

- [ ] **Step 2: Esegui e verifica fallimento**

Run: `npm test`
Expected: FAIL — `createRoute` non esiste.

- [ ] **Step 3: Scrivi `src/lib/stores/route.js`**

```js
import { writable } from 'svelte/store';

/**
 * Store di routing Home/Game basato su History API.
 * @param {{history?: History, target?: Window}} [deps]
 */
export function createRoute(deps = {}) {
  const history = deps.history ?? (typeof window !== 'undefined' ? window.history : null);
  const target = deps.target ?? (typeof window !== 'undefined' ? window : null);

  const { subscribe, set } = writable({ view: 'home' });

  function open(sceneId) {
    set({ view: 'game', sceneId });
    history?.pushState({ sceneId }, '', `#/scene/${sceneId}`);
  }

  function home() {
    // torna indietro nello stack così lo stato history resta coerente
    history?.back();
  }

  // il tasto Indietro (o back()) emette popstate: se non c'è sceneId, siamo in Home
  target?.addEventListener('popstate', (ev) => {
    const sceneId = ev?.state?.sceneId;
    set(sceneId ? { view: 'game', sceneId } : { view: 'home' });
  });

  return { subscribe, open, home };
}
```

- [ ] **Step 4: Esegui i test**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: store route Home/Game su History API"
```

---

## Task 3: Home, SceneCard e wiring del routing

**Files:**
- Create: `src/components/SceneCard.svelte`, `src/components/Home.svelte`
- Modify: `src/App.svelte`, `src/components/GameScreen.svelte`

Verifica manuale nel browser (logica routing già coperta da Task 2).

- [ ] **Step 1: `src/components/SceneCard.svelte`**

```svelte
<script>
  export let scene;
  export let onOpen = (_id) => {};
</script>

<button
  on:click={() => onOpen(scene.id)}
  style="border:none;cursor:pointer;padding:0;border-radius:14px;overflow:hidden;
         height:96px;position:relative;background:{scene.cover};
         box-shadow:0 3px 0 #d9b877;">
  <span style="position:absolute;left:10px;bottom:8px;color:#fff;font-weight:700;
               font-size:14px;text-shadow:0 1px 3px #00000088;">{scene.name}</span>
</button>
```

- [ ] **Step 2: `src/components/Home.svelte`**

```svelte
<script>
  import SceneCard from './SceneCard.svelte';
  import { scenes } from '../lib/data/scenes.js';
  export let onOpen = (_id) => {};
</script>

<header style="padding:16px 16px 6px;display:flex;align-items:center;gap:8px;">
  <div style="font-family:Georgia,serif;font-size:22px;font-weight:700;color:#7a5a2e;">⚜ Bardo</div>
</header>

<div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;padding:10px 16px 24px;">
  {#each scenes as scene}
    <SceneCard {scene} {onOpen} />
  {/each}
</div>
```

- [ ] **Step 3: Riscrivi `src/App.svelte`**

```svelte
<script>
  import { createRoute } from './lib/stores/route.js';
  import { getScene } from './lib/data/scenes.js';
  import Home from './components/Home.svelte';
  import GameScreen from './components/GameScreen.svelte';

  const route = createRoute();
</script>

{#if $route.view === 'game'}
  {#key $route.sceneId}
    <GameScreen scene={getScene($route.sceneId)} onBack={() => route.home()} />
  {/key}
{:else}
  <Home onOpen={(id) => route.open(id)} />
{/if}
```

Nota: `{#key $route.sceneId}` forza il rimontaggio di `GameScreen` (nuovo engine/mixer) quando cambia scena.

- [ ] **Step 4: Aggiorna `src/components/GameScreen.svelte`** — aggiungi un header con tasto Indietro. Sostituisci il file:

```svelte
<script>
  import { onMount } from 'svelte';
  import { AudioEngine } from '../lib/audio/AudioEngine.js';
  import { createMixer } from '../lib/stores/mixer.js';
  import NowPlaying from './NowPlaying.svelte';
  import IntensityTabs from './IntensityTabs.svelte';
  import Mixer from './Mixer.svelte';
  import OneShotBar from './OneShotBar.svelte';

  export let scene;
  export let onBack = () => {};

  const engine = new AudioEngine();
  const mixer = createMixer(engine);

  onMount(() => mixer.load(scene));
</script>

<div style="padding:10px 16px 0;">
  <button on:click={onBack}
    style="border:none;cursor:pointer;background:#ffffff88;color:var(--ink);
           padding:8px 14px;border-radius:20px;font-size:13px;font-weight:600;">← Home</button>
</div>

<NowPlaying name={scene.name} />
<IntensityTabs value={$mixer.intensity} onChange={(l) => mixer.setIntensity(l)} />

<div style="text-align:center;margin:4px 0 12px;">
  <button on:click={() => mixer.togglePlay()}
    style="border:none;cursor:pointer;background:var(--amber);color:#4a3410;font-weight:700;
           padding:12px 28px;border-radius:24px;font-size:15px;box-shadow:0 3px 0 #d9a85a;">
    {$mixer.playing ? '⏸ Pausa' : '▶ Play'}
  </button>
</div>

<Mixer {scene} state={$mixer} onMaster={(v) => mixer.setMaster(v)} onLayer={(id, v) => mixer.setLayerVolume(id, v)} />
<OneShotBar {scene} onPlay={(id) => mixer.oneShot(id)} />
```

- [ ] **Step 5: Verifica**

Run: `npm test`
Expected: 11+ test verdi (nessuna regressione; Task 1/2 hanno aggiunto test).

Run: `npm run build`
Expected: build pulita, nessun errore Svelte. Paste del tail.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: Home con griglia scene + routing Home/Game e tasto Indietro"
```

---

## Task 4: Pausa vera + HTML5 per-layer + unload one-shot

**Files:**
- Modify: `src/lib/audio/AudioEngine.js`, `src/lib/stores/mixer.js`
- Modify test: `src/lib/audio/AudioEngine.test.js`

- [ ] **Step 1: Aggiungi test** — appendi al describe "AudioEngine play / intensità / oneshot" in `src/lib/audio/AudioEngine.test.js`:

```js
  it('pause() mette in pausa tutti i layer senza stop', () => {
    engine.play();
    engine.pause();
    // ogni layer riceve pause(), non stop()
    for (const id of ['tavern-explore', 'crowd', 'fire']) {
      expect(engine._layerHowl(id).pause).toHaveBeenCalled();
    }
  });

  it('musica creata html5:true, ambient html5:false', () => {
    expect(engine._layerHowl('tavern-explore').opts.html5).toBe(true);
    expect(engine._layerHowl('crowd').opts.html5).toBe(false);
    expect(engine._layerHowl('fire').opts.html5).toBe(false);
  });

  it('playOneShot fa unload del precedente stesso id', () => {
    engine.playOneShot('door');
    const first = fake.created.at(-1);
    engine.playOneShot('door');
    expect(first.unload).toHaveBeenCalled();
  });
```

Aggiungi `pause: vi.fn()` all'oggetto howl finto in `makeFakeHowl` (nel test file): individua la definizione dell'oggetto `howl` dentro `makeFakeHowl` e aggiungi la riga `pause: vi.fn(),` accanto a `play`, `stop`, `unload`, `fade`, `once`, `volume`.

- [ ] **Step 2: Esegui e verifica fallimento**

Run: `npm test`
Expected: FAIL — `pause` non esiste; `opts.html5` di ambient è ancora `true`; oneshot non fa unload.

- [ ] **Step 3: Modifica `src/lib/audio/AudioEngine.js`**

3a. `_makeHowl` accetta il flag html5 (default true):
```js
  /** @param {AudioRef} ref */
  _makeHowl(ref, { volume, html5 = true }) {
    return this._factory({ src: ref.src, loop: !!ref.loop, html5, volume });
  }
```

3b. In `loadScene`, gli ambient passano `html5:false`:
```js
    for (const ref of scene.ambient) {
      this._layers.set(ref.id, { howl: this._makeHowl(ref, { volume: 0, html5: false }), volume: 0 });
    }
```
(La musica in `loadScene` resta `_makeHowl(musicRef, { volume: this._master })` → html5 default true.)

3c. Aggiungi il metodo `pause()` dopo `stop()`:
```js
  pause() {
    for (const { howl } of this._layers.values()) howl.pause();
  }
```

3d. In `playOneShot`, fai unload del precedente e usa Web Audio (html5:false):
```js
  /** @param {string} sfxId */
  playOneShot(sfxId) {
    if (!this._scene) return;
    const ref = this._scene.oneshots.find((s) => s.id === sfxId);
    if (!ref) return;
    const prev = this._oneshots.get(sfxId);
    if (prev) prev.unload();
    const howl = this._factory({ src: ref.src, loop: false, html5: false, volume: this._master });
    this._oneshots.set(sfxId, howl);
    howl.play();
  }
```

- [ ] **Step 4: Modifica `src/lib/stores/mixer.js`** — `togglePlay` usa `pause()` invece di `stop()`:

```js
  function togglePlay() {
    update((s) => {
      const playing = !s.playing;
      if (playing) engine.play(); else engine.pause();
      return { ...s, playing };
    });
  }
```

- [ ] **Step 4b: Aggiorna il test mixer** `src/lib/stores/mixer.test.js` — ora la pausa chiama `engine.pause()`, non `engine.stop()`. Due modifiche:
  1. In `fakeEngine()` aggiungi `pause: vi.fn()` (accanto a `play`/`stop`).
  2. Nel test "toggle play aggiorna playing e chiama engine", cambia la seconda asserzione da `expect(engine.stop).toHaveBeenCalled();` a `expect(engine.pause).toHaveBeenCalled();`.

- [ ] **Step 5: Esegui i test**

Run: `npm test`
Expected: PASS (nuovi test verdi; il test esistente "setIntensity crea il nuovo layer musicale e fa il crossfade" resta verde — la musica è ancora html5 default true).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "fix: pausa vera + Web Audio per ambient/SFX + unload one-shot"
```

---

## Task 5: Wrapper MediaSession + integrazione

**Files:**
- Create: `src/lib/audio/media-session.js`
- Test: `src/lib/audio/media-session.test.js`
- Modify: `src/lib/stores/mixer.js`, `src/components/GameScreen.svelte`

- [ ] **Step 1: Scrivi il test** `src/lib/audio/media-session.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { createMediaSession } from './media-session.js';

function fakeNav() {
  return {
    mediaSession: {
      metadata: null,
      playbackState: 'none',
      setActionHandler: vi.fn()
    }
  };
}

describe('media-session', () => {
  beforeEach(() => {
    // MediaMetadata finto globale
    globalThis.MediaMetadata = class { constructor(init) { Object.assign(this, init); } };
  });

  it('no-op se mediaSession assente', () => {
    const ms = createMediaSession({});
    expect(() => { ms.setScene({ name: 'x', cover: 'y' }); ms.setPlaybackState('playing'); }).not.toThrow();
    expect(ms.supported).toBe(false);
  });

  it('setScene imposta metadata; setPlaybackState imposta lo stato', () => {
    const nav = fakeNav();
    const ms = createMediaSession(nav);
    ms.setScene({ name: 'Taverna', cover: 'linear-gradient(...)' });
    expect(nav.mediaSession.metadata.title).toBe('Taverna');
    ms.setPlaybackState('playing');
    expect(nav.mediaSession.playbackState).toBe('playing');
  });

  it('setHandlers registra play/pause/stop', () => {
    const nav = fakeNav();
    const ms = createMediaSession(nav);
    const onPlay = vi.fn(), onPause = vi.fn(), onStop = vi.fn();
    ms.setHandlers({ onPlay, onPause, onStop });
    const calls = Object.fromEntries(nav.mediaSession.setActionHandler.mock.calls);
    expect(calls.play).toBeTypeOf('function');
    expect(calls.pause).toBeTypeOf('function');
    expect(calls.stop).toBeTypeOf('function');
    calls.play(); calls.pause(); calls.stop();
    expect(onPlay).toHaveBeenCalled();
    expect(onPause).toHaveBeenCalled();
    expect(onStop).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Esegui e verifica fallimento**

Run: `npm test`
Expected: FAIL — `createMediaSession` non esiste.

- [ ] **Step 3: Scrivi `src/lib/audio/media-session.js`**

```js
/**
 * Wrapper feature-detected su navigator.mediaSession.
 * @param {Navigator|Object} [nav]
 */
export function createMediaSession(nav = typeof navigator !== 'undefined' ? navigator : {}) {
  const ms = nav && nav.mediaSession ? nav.mediaSession : null;
  const supported = !!ms;

  function setScene(scene) {
    if (!ms || typeof MediaMetadata === 'undefined') return;
    ms.metadata = new MediaMetadata({
      title: scene.name,
      artist: 'Bardo',
      // cover può essere un gradiente CSS (non un URL immagine): passiamo artwork solo se è un path
      artwork: typeof scene.cover === 'string' && scene.cover.startsWith('/')
        ? [{ src: scene.cover, sizes: '512x512', type: 'image/png' }]
        : []
    });
  }

  function setPlaybackState(state) {
    if (!ms) return;
    ms.playbackState = state; // 'playing' | 'paused' | 'none'
  }

  function setHandlers({ onPlay, onPause, onStop }) {
    if (!ms) return;
    ms.setActionHandler('play', () => onPlay && onPlay());
    ms.setActionHandler('pause', () => onPause && onPause());
    ms.setActionHandler('stop', () => onStop && onStop());
  }

  return { supported, setScene, setPlaybackState, setHandlers };
}
```

- [ ] **Step 4: Esegui i test** → PASS.

- [ ] **Step 5: Integra nello store** `src/lib/stores/mixer.js` — `createMixer` accetta deps opzionali e aggiorna MediaSession. Sostituisci la firma e i metodi rilevanti:

5a. Firma + destrutturazione (sostituisci `export function createMixer(engine) {`):
```js
/**
 * @param {import('../audio/AudioEngine.js').AudioEngine} engine
 * @param {{mediaSession?: {setScene:Function,setPlaybackState:Function,setHandlers:Function}}} [deps]
 */
export function createMixer(engine, deps = {}) {
  const mediaSession = deps.mediaSession ?? null;
```

5b. In `load`, dopo `set({...})`, imposta scena + handler MediaSession:
```js
  function load(scene) {
    engine.loadScene(scene);
    _playing = false;
    const layers = {};
    for (const l of scene.ambient) layers[l.id] = 0;
    set({ scene, playing: false, master: 1, intensity: 'explore', layers });
    if (mediaSession) {
      mediaSession.setScene(scene);
      mediaSession.setHandlers({
        onPlay: () => { if (!isPlaying()) togglePlay(); },
        onPause: () => { if (isPlaying()) togglePlay(); },
        onStop: () => stop()
      });
    }
  }
```

5c. Aggiungi un helper `isPlaying` e un `stop` a livello di store (dopo `load`), e aggiorna `togglePlay` per riflettere lo stato in MediaSession:
```js
  let _playing = false;
  function isPlaying() { return _playing; }

  function togglePlay() {
    update((s) => {
      const playing = !s.playing;
      if (playing) engine.play(); else engine.pause();
      _playing = playing;
      mediaSession?.setPlaybackState(playing ? 'playing' : 'paused');
      return { ...s, playing };
    });
  }

  function stop() {
    engine.stop();
    _playing = false;
    mediaSession?.setPlaybackState('none');
    update((s) => ({ ...s, playing: false }));
  }
```

5d. Esporta anche `stop` nel return:
```js
  return { subscribe, load, togglePlay, setMaster, setLayerVolume, setIntensity, oneShot, stop };
```

- [ ] **Step 6: Integra in `GameScreen.svelte`** — costruisci il MediaSession e passalo al mixer. Aggiorna gli import + costruzione:
```js
  import { AudioEngine } from '../lib/audio/AudioEngine.js';
  import { createMixer } from '../lib/stores/mixer.js';
  import { createMediaSession } from '../lib/audio/media-session.js';
  ...
  const engine = new AudioEngine();
  const mixer = createMixer(engine, { mediaSession: createMediaSession() });
```

- [ ] **Step 7: Verifica**

Run: `npm test` → tutti verdi (i test mixer esistenti usano `createMixer(engine)` senza deps: `deps` default `{}`, `mediaSession` null, quindi nessun cambio di comportamento — verifica che i 4 test mixer restino verdi).

Run: `npm run build` → pulita.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: MediaSession (metadata + controlli lock screen)"
```

---

## Task 6: Wrapper Wake Lock + toggle UI

**Files:**
- Create: `src/lib/audio/wake-lock.js`
- Test: `src/lib/audio/wake-lock.test.js`
- Modify: `src/lib/stores/mixer.js`, `src/components/GameScreen.svelte`

- [ ] **Step 1: Scrivi il test** `src/lib/audio/wake-lock.test.js`:

```js
import { describe, it, expect, vi } from 'vitest';
import { createWakeLock } from './wake-lock.js';

describe('wake-lock', () => {
  it('no-op e supported=false se assente', async () => {
    const wl = createWakeLock({});
    expect(wl.supported).toBe(false);
    await expect(wl.enable()).resolves.toBeUndefined();
    await expect(wl.disable()).resolves.toBeUndefined();
  });

  it('enable richiede lo screen lock; disable lo rilascia', async () => {
    const sentinel = { release: vi.fn().mockResolvedValue(undefined) };
    const nav = { wakeLock: { request: vi.fn().mockResolvedValue(sentinel) } };
    const wl = createWakeLock(nav);
    expect(wl.supported).toBe(true);
    await wl.enable();
    expect(nav.wakeLock.request).toHaveBeenCalledWith('screen');
    await wl.disable();
    expect(sentinel.release).toHaveBeenCalled();
  });

  it('enable due volte non crea due sentinel', async () => {
    const sentinel = { release: vi.fn().mockResolvedValue(undefined) };
    const nav = { wakeLock: { request: vi.fn().mockResolvedValue(sentinel) } };
    const wl = createWakeLock(nav);
    await wl.enable();
    await wl.enable();
    expect(nav.wakeLock.request).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: Esegui e verifica fallimento** → FAIL (`createWakeLock` non esiste).

- [ ] **Step 3: Scrivi `src/lib/audio/wake-lock.js`**

```js
/**
 * Wrapper feature-detected su navigator.wakeLock.
 * @param {Navigator|Object} [nav]
 */
export function createWakeLock(nav = typeof navigator !== 'undefined' ? navigator : {}) {
  const api = nav && nav.wakeLock ? nav.wakeLock : null;
  const supported = !!api;
  let sentinel = null;

  async function enable() {
    if (!api || sentinel) return;
    try {
      sentinel = await api.request('screen');
      sentinel.addEventListener?.('release', () => { sentinel = null; });
    } catch {
      sentinel = null;
    }
  }

  async function disable() {
    if (!sentinel) return;
    try { await sentinel.release(); } catch { /* ignore */ }
    sentinel = null;
  }

  return { supported, enable, disable };
}
```

- [ ] **Step 4: Esegui i test** → PASS.

- [ ] **Step 5: Integra nello store** `src/lib/stores/mixer.js`:

5a. Estendi deps + stato `keepAwake`. Aggiorna la destrutturazione deps:
```js
export function createMixer(engine, deps = {}) {
  const mediaSession = deps.mediaSession ?? null;
  const wakeLock = deps.wakeLock ?? null;
```

5b. Aggiungi `keepAwake:false` allo stato iniziale del writable e nello `set` di `load`:
```js
  const { subscribe, set, update } = writable({
    scene: null, playing: false, master: 1, intensity: 'explore', layers: {}, keepAwake: false
  });
  ...
  // dentro load(), nello stato impostato:
    set({ scene, playing: false, master: 1, intensity: 'explore', layers, keepAwake: false });
```

5c. In `togglePlay`, gestisci il wake lock in base a keepAwake:
```js
  function togglePlay() {
    update((s) => {
      const playing = !s.playing;
      if (playing) engine.play(); else engine.pause();
      _playing = playing;
      mediaSession?.setPlaybackState(playing ? 'playing' : 'paused');
      if (wakeLock) { (playing && s.keepAwake) ? wakeLock.enable() : wakeLock.disable(); }
      return { ...s, playing };
    });
  }
```

5d. Aggiungi `setKeepAwake` (dopo `stop`) e in `stop` rilascia il lock:
```js
  function setKeepAwake(on) {
    update((s) => {
      if (wakeLock) { (on && s.playing) ? wakeLock.enable() : wakeLock.disable(); }
      return { ...s, keepAwake: on };
    });
  }
```
E in `stop()` aggiungi `wakeLock?.disable();` prima dell'`update`.

5e. Esporta `setKeepAwake`:
```js
  return { subscribe, load, togglePlay, setMaster, setLayerVolume, setIntensity, oneShot, stop, setKeepAwake };
```

- [ ] **Step 6: UI + costruzione in `GameScreen.svelte`**

6a. Costruzione:
```js
  import { createWakeLock } from '../lib/audio/wake-lock.js';
  ...
  const wake = createWakeLock();
  const mixer = createMixer(engine, { mediaSession: createMediaSession(), wakeLock: wake });
```

6b. Aggiungi il toggle sotto il bottone Play (mostralo solo se supportato):
```svelte
{#if wake.supported}
  <div style="text-align:center;margin:-4px 0 10px;">
    <label style="font-size:12px;color:var(--ink-soft);cursor:pointer;">
      <input type="checkbox" checked={$mixer.keepAwake}
             on:change={(e) => mixer.setKeepAwake(e.target.checked)} />
      Tieni schermo acceso
    </label>
  </div>
{/if}
```

- [ ] **Step 7: Verifica**

Run: `npm test` → tutti verdi (i test mixer esistenti passano `createMixer(engine)`; `wakeLock` null → rami no-op; `keepAwake` aggiunto allo stato non rompe le asserzioni esistenti che controllano campi specifici).

Run: `npm run build` → pulita.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: Wake Lock con toggle 'tieni schermo acceso'"
```

---

## Task 7: Audio CC0 reale per le 4 scene + registro licenze

**Files:**
- Create: `public/audio/<sceneId>/*.webm` (e/o `.m4a`), `src/lib/data/licenses.json`

Questa task NON ha unit test (gli asset sono mockati nei test): la verifica è che i file esistano, il build li includa e suonino nel browser. È l'unica task a rischio "sourcing" — vedi fallback.

File attesi per scena (nomi = quelli in `scenes.js`), formato `.webm` (Opus) preferito, `.m4a` opzionale:
- **tavern:** explore, combat, victory, crowd, fire, door, cheers
- **forest:** explore, combat, victory, wind, birds, twig, owl
- **city:** explore, combat, victory, market, people, bell, cart
- **dungeon:** explore, combat, victory, drip, wind, chain, growl

- [ ] **Step 1: Procura audio CC0 a download diretto.** Fonti consigliate (verificare licenza CC0/CC-BY per ogni file e annotarla):
  - Musica loop: OpenGameArt (filtro CC0), incompetech.com (Kevin MacLeod, CC-BY → richiede credit).
  - Ambient/SFX: Pixabay (sound-effects, license "Pixabay Content License", uso libero), OpenGameArt (CC0).
  Scarica in `public/audio/<sceneId>/<nome>.webm`. Se un file sorgente è mp3/wav, convertilo/ritaglialo (loop seamless per music/ambient). Tieni i file piccoli (Opus ~64-96kbps mono per ambient).

- [ ] **Step 2: Se il download automatico fallisce (login/API richiesti), applica il fallback in ordine:**
  1. Riduci a meno file per scena (min: 1 music explore + 1 ambient + 1 oneshot) — aggiorna `scenes.js` di conseguenza (rimuovi i ref senza file) così l'app non punta a 404.
  2. Se proprio non recuperabile in automatico, LASCIA i ref in `scenes.js` e segnala nel report quali file mancano, così il controller/utente li droppa a mano. NON inventare audio.

- [ ] **Step 3: Compila `src/lib/data/licenses.json`** con una entry per ogni file scaricato:
```json
[
  { "file": "audio/tavern/explore.webm", "source": "https://...", "license": "CC0", "author": "" }
]
```
(Array vuoto `[]` se nessun file è stato recuperato.)

- [ ] **Step 4: Verifica presenza file.** Run: `ls -R public/audio` — elenca ciò che c'è. Paste dell'output nel report (indica esplicitamente quali dei file attesi mancano).

- [ ] **Step 5: Commit** (solo se almeno alcuni file sono stati aggiunti):
```bash
git add public/audio src/lib/data/licenses.json
git commit -m "assets: audio CC0 reale per le scene + registro licenze"
```

---

## Task 8: Precache audio (offline) + verifica

**Files:**
- Modify: `vite.config.js`
- Modify: `README.md`

- [ ] **Step 1: Estendi Workbox per precache degli audio.** In `vite.config.js`, aggiungi `workbox.globPatterns` dentro l'opzione `VitePWA`:
```js
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,webmanifest,webm,m4a}'],
        maximumFileSizeToCacheInBytes: 6 * 1024 * 1024
      },
      manifest: { /* invariato */ }
    })
```
(Mantieni il blocco `manifest` esistente così com'è.)

- [ ] **Step 2: Build e verifica precache.** Run: `npm run build`
Expected: build pulita; l'output PWA mostra un numero di `precache entries` maggiore (include gli audio presenti in `public/audio`). Paste del tail. Run `ls dist/audio` per confermare che gli audio siano copiati in dist.

- [ ] **Step 3: Aggiorna `README.md`** — aggiungi in fondo:
```markdown

## Contenuti audio
Gli audio in `public/audio/` sono CC0/royalty-free; vedi `src/lib/data/licenses.json` per fonti e licenze. I contenuti CC-BY richiedono attribuzione (schermata Credits prevista in una fase successiva).
```

- [ ] **Step 4: Regressione finale.** Run: `npm test` → tutti i test verdi.

- [ ] **Step 5: Commit**
```bash
git add -A
git commit -m "feat: precache audio per uso offline + nota licenze nel README"
```

---

## Definition of Done (Fase 2)

- `npm test` verde (route, pause/resume, html5-per-layer, oneshot unload, media-session, wake-lock, + regressioni).
- `npm run build` pulita; audio (se presenti) nel precache; app shell + audio offline.
- Home con 4 scene; apertura scena; tasto Indietro Android/browser torna alla Home.
- Pausa vera (riprende da dov'era); nessun warning "HTML5 Audio pool exhausted".
- MediaSession attivo (metadata + play/pause/stop); Wake Lock toggle quando supportato.
- Audio CC0 reale nelle scene (o fallback documentato nel report se un sourcing è fallito).

## Verifiche manuali (controller, browser)

- Home → apri scena → `←` / tasto Indietro Android riporta a Home.
- Play → suono reale; alza ambient; cambia intensità (crossfade); Pausa → riprende da dov'era.
- DevTools/telefono Android: controlli su notifica/lock screen (play/pause/stop).
- Toggle "tieni schermo acceso" attivo (dove supportato).
- Offline (Network offline / senza rete): l'app e gli audio già in precache continuano a funzionare.

## Note / carry-over verso Fase 3+

- Download di pacchetti aggiuntivi + gestione spazio + rimozione dal device → Fase 3.
- Scene builder + preset (IndexedDB) → Fase 4.
- Schermata Credits (per CC-BY), grafica/icone finali, `manifest.lang:'it'`, QA iOS approfondito → Fase 5.
